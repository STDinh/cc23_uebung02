variables:
    GREET: "Mundo"

stages:
    - info
    - test

docker:
    stage: info
    image: "bitnami/kubectl:1.18"
        entrypoint: ["sh"]
    script:
        - kubectl version --kubeconfig=$KUBE_CONFIG
