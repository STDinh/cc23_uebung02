stages:
  - build
  - test



builder1:
  stage: build
  script:
    - echo "this is building"
    - hostname
    - mkdir builds
    - touch builds/data.txt
    - echo $(hostname) > builds/data-$(uuidgen).txt
  artifacts:
    paths:
      - builds/

builder2:
  stage: build
  script:
    - echo "this is building"
    - hostname
    - mkdir builds
    - touch builds/data.txt
    - echo $(hostname) > builds/data-$(uuidgen).txt
  artifacts:
    paths:
      - builds/

builder3:
  stage: build
  script:
    - echo "this is building"
    - hostname
    - mkdir builds
    - touch builds/data.txt
    - echo $(hostname) > builds/data-$(uuidgen).txt
  artifacts:
    paths:
      - builds/

tester:
  stage: test
  script:
    - echo "this is testing"
