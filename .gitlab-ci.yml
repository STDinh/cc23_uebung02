variables:
    GREET: "Mundo"

stages:
    - info

docker:
    stage: info
    image: 
        name: "bitnami/kubectl:1.18"
        entrypoint: ["/bin/bash"]
    script:
        - which kubectl
        - kubectl version --kubeconfig=$KUBE_CONFIG
