stages:
  - build
  - test

artifacts:
  paths:
    - builds/

builder1:
  stage: build
  script:
    - echo "this is building"
    - hostname
    - mkdir builds
    - touch builds/data.txt
    - echo $(hostname) > builds/data-$(uuidgen).txt

builder2:
  stage: build
  script:
    - echo "this is building"
    - hostname
    - mkdir builds
    - touch builds/data.txt
    - echo $(hostname) > builds/data-$(uuidgen).txt

builder3:
  stage: build
  script:
    - echo "this is building"
    - hostname
    - mkdir builds
    - touch builds/data.txt
    - echo $(hostname) > builds/data-$(uuidgen).txt

tester:
  stage: test
  script:
    - echo "this is testing"
